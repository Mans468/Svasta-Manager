// Rendu quand aucune route n'a été interceptée dans ce slot
// (ex. navigation directe sur "/", ou arrivée via un lien direct
// sur une page qui a son propre fallback plein écran).
export default function Default() {
    return null;
}
